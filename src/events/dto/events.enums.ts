export const eventAccessModeValues = ['open', 'registered_only'] as const;

export const eventRegistrationStatusValues = [
  'confirmed',
  'cancelled',
  'waitlisted',
] as const;

export const eventStatusValues = ['draft', 'published', 'cancelled'] as const;
